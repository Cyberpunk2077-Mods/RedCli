name: Build Mod Package

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

env:
  TARGET_PLATFORM: windows
  TARGET_ARCH: x64
  # Reserved, disabled targets: windows_arm, macos_x64, macos_arm, linux_x64, linux_arm.
  MOD_NAME: RedCli

jobs:
  package:
    runs-on: windows-2025
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: dart-lang/setup-dart@v1

      - name: Apply tag version to source
        if: github.ref_type == 'tag'
        shell: pwsh
        run: |
          $version = $env:GITHUB_REF_NAME.TrimStart('v')
          (Get-Content pubspec.yaml -Raw) -replace '(?m)^version:\s*.+$', "version: $version" | Set-Content pubspec.yaml -NoNewline
          if (-not (Select-String -Path pubspec.yaml -Pattern "^version:\s*$([regex]::Escape($version))\s*$" -Quiet)) {
            throw "Failed to set pubspec.yaml version to $version"
          }

      - name: Setup project
        run: dart pub get

      - name: Build windows_x64
        run: dart compile exe bin/main.dart -o red-cli.exe

      - name: Stage package contents
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path package | Out-Null
          Copy-Item red-cli.exe, LICENSE, README.md -Destination package -Force

      - name: Normalize artifact name
        id: meta
        shell: pwsh
        run: |
          $version = if ($env:GITHUB_REF_TYPE -eq 'tag') {
            $env:GITHUB_REF_NAME.TrimStart('v')
          } else {
            $stable = @(git tag -l --sort=-v:refname | Where-Object { $_ -match '^v?\d+\.\d+\.\d+$' })
            $any = @(git tag -l --sort=-v:refname | Where-Object { $_ -match '^v?\d' })
            $tag = @($stable + $any) | Select-Object -First 1
            if ($tag) { $tag.TrimStart('v') } else { '0.0.0' }
          }
          $suffix = if ($env:GITHUB_REF_TYPE -eq 'tag') {
            ''
          } else {
            "_$(Get-Date -Format 'yyyy.M').$((git rev-list --count --since="$(Get-Date -Day 1 -Format yyyy-MM-dd)" HEAD))"
          }
          $artifact = "${env:MOD_NAME}_v${version}_${env:TARGET_PLATFORM}_${env:TARGET_ARCH}${suffix}.zip"
          Compress-Archive -Path package/* -DestinationPath $artifact -CompressionLevel Optimal -Force
          "artifact=$artifact" >> $env:GITHUB_OUTPUT

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.meta.outputs.artifact }}
          path: ${{ steps.meta.outputs.artifact }}
          if-no-files-found: error

      - name: Create draft release
        if: github.ref_type == 'tag'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$env:GITHUB_REF_NAME" "${{ steps.meta.outputs.artifact }}" --draft --generate-notes --verify-tag
